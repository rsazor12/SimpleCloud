using AutoMapper;

namespace Payment_SimpleCloud_MicroservicesMessageBroker.Application.Common.Mappings
{
    public interface IMapFrom<T>
    {   
        void Mapping(Profile profile) => profile.CreateMap(typeof(T), GetType());
    }
}
