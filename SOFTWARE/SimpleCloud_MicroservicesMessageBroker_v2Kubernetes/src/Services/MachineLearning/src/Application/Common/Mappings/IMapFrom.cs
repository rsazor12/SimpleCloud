using AutoMapper;

namespace MachineLearning_SimpleCloud_MicroservicesHttp.Application.Common.Mappings
{
    public interface IMapFrom<T>
    {   
        void Mapping(Profile profile) => profile.CreateMap(typeof(T), GetType());
    }
}
